<%= content_tag_for :li, preset, :class => cycle('left', 'right') do %>
  <%= preset.name %>
  <pre><%= preset.parameters %></pre>
  <%= link_to 'Edit', edit_preset_path(preset) %> - 
  <%= link_to 'Delete', api_preset_path(preset), :method => :delete %>
<% end %>